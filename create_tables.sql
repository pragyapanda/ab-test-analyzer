CREATE TABLE users_a (
    user_id INT PRIMARY KEY,
    signup_date DATE,
    signup_method VARCHAR(50)
);

CREATE TABLE users_b (
    user_id INT PRIMARY KEY,
    signup_date DATE,
    signup_method VARCHAR(50)
);
